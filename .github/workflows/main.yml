on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Optimistic
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v4
      - name: Optimistic action step
        uses: ./Backend # Uses an action in the root directory
        id: Optimistic_Test
